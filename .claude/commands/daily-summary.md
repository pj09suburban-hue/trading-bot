---
description: Run the daily summary workflow locally (uses local .env)
---

Run the daily summary workflow. Credentials come from local .env. Follow routines/daily-summary.md exactly, minus the env-var block and the commit-and-push step (local runs don't auto-push).
