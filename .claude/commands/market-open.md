---
description: Run the market-open execution workflow locally (uses local .env)
---

Run the market-open workflow. Credentials come from local .env. Follow routines/market-open.md exactly, minus the env-var block and the commit-and-push step (local runs don't auto-push).
