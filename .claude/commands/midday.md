---
description: Run the midday scan workflow locally (uses local .env)
---

Run the midday scan workflow. Credentials come from local .env. Follow routines/midday.md exactly, minus the env-var block and the commit-and-push step (local runs don't auto-push).
