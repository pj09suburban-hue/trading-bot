---
description: Run the pre-market research workflow locally (uses local .env)
---

Run the pre-market workflow. Credentials come from local .env. Follow routines/pre-market.md exactly, minus the env-var block and the commit-and-push step (local runs don't auto-push).
