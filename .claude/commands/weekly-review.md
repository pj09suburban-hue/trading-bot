---
description: Run the weekly review workflow locally (uses local .env)
---

Run the weekly review workflow. Credentials come from local .env. Follow routines/weekly-review.md exactly, minus the env-var block and the commit-and-push step (local runs don't auto-push).
